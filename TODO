1. Play with enabling docker bit, and each tool one at a time until the pi 3 is 100% CPU (network stats might be limited by pi NIC - rather than the actuall internet speed).
2. Move the internet stuff to a newer pi (e.g. Pi4), keeping the static website on the pi 3, so it can defintely be enabled.
